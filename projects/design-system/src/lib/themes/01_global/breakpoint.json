{
  "ob": {
    "g": {
      "breakpoint": {
        "token_family_docs": {
          "$description": "Base viewport breakpoint primitives. Source of truth for every viewport-related px value in the system; the only place literal breakpoint values live. Referenced by ob.g.configuration.mode.viewport ranges and from there into the semantic and component layers.",
          "$extensions": {
            "oblique": {
              "kind": "family_docs",
              "export": false
            }
          }
        },
        "xs": {
          "$type": "dimension",
          "$value": "0px",
          "$description": "Lower boundary of the xs (extra-small) viewport range."
        },
        "sm": {
          "$type": "dimension",
          "$value": "480px",
          "$description": "Lower boundary of the sm (small) viewport range."
        },
        "md": {
          "$type": "dimension",
          "$value": "768px",
          "$description": "Lower boundary of the md (medium) viewport range."
        },
        "lg": {
          "$type": "dimension",
          "$value": "1024px",
          "$description": "Lower boundary of the lg (large) viewport range."
        },
        "xl": {
          "$type": "dimension",
          "$value": "1440px",
          "$description": "Lower boundary of the xl (extra-large) viewport range."
        },
        "2xl": {
          "$type": "dimension",
          "$value": "1920px",
          "$description": "Lower boundary of the 2xl (extra-extra-large) viewport range."
        }
      }
    }
  }
}
