{
  "ob": {
    "g": {
      "mode": {
        "typography_context": {
          "token_family_docs": {
            "$description": "Typography-context mode — text rendering for UI versus reading content (interface / prose). Each value carries the CSS class string the application adds to a container. interface is the base.",
            "$extensions": {
              "oblique": {
                "kind": "family_docs",
                "export": false
              }
            }
          },
          "interface": {
            "selector": {
              "$type": "other",
              "$value": "default",
              "$description": "interface is the base typography-context mode — no class is applied. It is the first-paint default."
            }
          },
          "prose": {
            "selector": {
              "$type": "other",
              "$value": ".ob-typography-context-prose",
              "$description": "Developer-facing class. The application adds it to a container to activate prose typography context."
            }
          }
        }
      }
    }
  }
}
