# Figma Builders

Scripts that generate and rebuild living documentation tables in Figma via the MCP Desktop Bridge.

Each subfolder targets a specific token domain:

| Folder | Purpose |
|---|---|
| `color-tokens/` | DS-Token-Colors — 14 color token tables |

Future folders (planned):

- `typography-tokens/` — typography token documentation
