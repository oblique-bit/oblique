{
  "$schema": "http://json-schema.org/schema",
  "$id": "add-code-example",
  "title": "Oblique add code example schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the feature to document.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What's the name of the new example?"
    }
  },
  "required": ["name"]
}
