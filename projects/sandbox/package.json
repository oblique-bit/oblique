{
  "name": "sandbox",
  "version": "",
  "scripts": {
    "start": "ng serve",
    "test": "cd ../.. && ng test sandbox",
    "build": "ng build"
  },
  "dependencies": {
    "raw-loader": "^4.0.2"
  }
}
