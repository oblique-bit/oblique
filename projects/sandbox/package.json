{
  "name": "@oblique/sandbox",
  "version": "13.3.2",
  "scripts": {
    "start": "ng serve",
    "start-pams": "ng serve --configuration pams",
    "lint": "ts-node scripts/lint.ts",
    "format": "ts-node scripts/lint.ts --fix",
    "test": "cd ../.. && ng test sandbox",
    "test-ci": "cd ../.. && ng run sandbox:test:ci",
    "build": "ng build && ts-node scripts/post-build.ts",
    "release": "ts-node scripts/release.ts"
  },
  "dependencies": {
    "@angular/material": "^20.2.3",
    "@angular/material-luxon-adapter": "^20.2.3",
    "@oblique/oblique": "^13.3.2",
    "raw-loader": "^4.0.2"
  }
}
