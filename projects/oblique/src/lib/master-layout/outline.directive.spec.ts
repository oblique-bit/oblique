import {ObOutlineDirective} from './outline.directive';

describe('ObOutlineDirective', () => {
	it('should create an instance', () => {
		const directive = new ObOutlineDirective();
		expect(directive).toBeTruthy();
	});
});
