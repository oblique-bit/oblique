{
  "$schema": "../../../schema.json",
  "api": [
    {
      "name": "ObUnsavedChangesDirective",
      "selector": "[obUnsavedChanges]",
      "usage": "Applied on a <code>&lt;form&gt;</code> or any element with <code>ngModelGroup</code> directive. The host element needs an <code>id</code>.",
      "since": "2.0.0"
    },
    {
      "name": "UnsavedChangesGuard",
      "usage": "Apply to routes that have to be protected",
      "since": "2.0.0"
    }
  ]
}
