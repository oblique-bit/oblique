{
  "$schema": "../../../schema.json",
  "api": [
    {
      "name": "ObUnsavedChangesTabsDirective",
      "selector": "[obUnsavedChangesTabs]",
      "type": "directive",
      "usage": "Applied on a <code>&lt;form&gt;</code> or any element with <code>ngModelGroup</code> directive. The host element needs either an <code>id</code> or be wrapped int a <code>ngbNavItem</code> directive.",
      "since": "2.0.0",
      "updated": "6.0.0"
    },
    {
      "name": "UnsavedChangesGuard",
      "usage": "Apply to routes that have to be protected",
      "type": "injectable(guard)",
      "since": "2.0.0"
    }
  ]
}
