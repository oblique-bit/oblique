{
  "$schema": "http://json-schema.org/schema",
  "$id": "oblique-ng-add-schema",
  "title": "Oblique ng-add schema",
  "type": "object",
  "required": ["applicationOperator", "contact"],
  "project": {
    "$default": {
      "$source": "projectName"
    }
  },
  "properties": {
    "title": {
      "type": "string",
      "default": "Title",
      "description": "Application's title",
      "x-prompt": "[1 / 13] What is the title of the application? This is a mandatory information",
      "minLength": 1
    },
    "applicationOperator": {
      "type": "string",
      "x-prompt": "[2 / 13] Enter the name and address of the federal office or other entity that operates the application.\n           It accepts either text or a translation key, both of which support HTML.\n           This information will be wrapped in an <address> tag on the Accessibility Statement page: ",
      "minLength": 1
    },
    "contact": {
      "type": "string",
      "description": "One or more email address(es) and phone number(s) of the operator (comma-separated)",
      "x-prompt": "[3 / 13] Enter email address(es) and phone number(s) of the operator, in minimum one (separated by comma, e.g: info@example.org, support@example.org, +41 79 123 45 67, 031 555 44 33): ",
      "pattern": "^\\s*([+]?[(]?\\d+[)]?[\\d\\s()./-]*|[^@\\s]+@[^@\\s]+\\.[^@\\s]+)(\\s*,\\s*([+]?[(]?\\d+[)]?[\\d\\s()./-]*|[^@\\s]+@[^@\\s]+\\.[^@\\s]+))*\\s*$",
      "minLength": 1
    },
    "hasLanguageInUrl": {
      "type": "boolean",
      "default": false,
      "description": "Use a language path parameter",
      "x-prompt": "[4 / 13] Use a language parameter in the URL?"
    },
    "ajv": {
      "type": "boolean",
      "default": true,
      "description": "Add Ajv dependency",
      "x-prompt": "[5 / 13] Add schema validation? This enables form validation based on a schema delivered by the server."
    },
    "unknownRoute": {
      "type": "boolean",
      "default": true,
      "description": "Unknown route management",
      "x-prompt": "[6 / 13] Add the ObUnknownRouteModule? It will display custom 404 pages instead of redirecting to the home page?"
    },
    "httpInterceptors": {
      "type": "boolean",
      "default": true,
      "description": "Http interceptor",
      "x-prompt": "[7 / 13] Add the ObHttpApiInterceptor? It will show a spinner and handle errors on API calls"
    },
    "banner": {
      "type": "boolean",
      "default": false,
      "description": "Banner to show current environment",
      "x-prompt": "[8 / 13] Add the ObBanner? It will show the current environment in the header.\n           This feature is only available if at least one environment will be defined at the next step."
    },
    "environments": {
      "type": "string",
      "default": "local dev ref test abn prod",
      "description": "Environment files",
      "x-prompt": "[9 / 13] Choose the application's environments. Use a whitespace separated list or leave empty to skip the feature.\n           'local' will create an 'environment.ts' file, all other environments will create a corresponding 'environment.<env>.ts' file.\n           Those files are necessary for the banner feature to work."
    },
    "externalLink": {
      "type": "boolean",
      "default": true,
      "description": "External link",
      "x-prompt": "[10 / 13] Add the ObExternalLinkModule? It will transparently and automatically enhance external links."
    },
    "prefix": {
      "type": "string",
      "default": "app",
      "description": "Prefix configuration",
      "x-prompt": "[11 / 13] Choose a prefix for components and directives' selectors. Leave empty for no prefix."
    },
    "eslint": {
      "type": "boolean",
      "default": true,
      "description": "ESLint & Prettier",
      "x-prompt": "[12 / 13] Add ESLint & Prettier and configure them like the Oblique team?"
    },
    "husky": {
      "type": "boolean",
      "default": true,
      "description": "Husky configuration",
      "x-prompt": "[13 / 13] Add git hooks to automatically format changed files?"
    }
  }
}
