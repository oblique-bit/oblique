name: Build and publish to npm

on:
  push:
    branches: [master, "feature/OUI-4515-publish-from-github"]
  pull_request:
    branches: [master, develop]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false
      - run: npm install -g npm@latest
      - run: npm ci
      - run: npm run build --workspace=projects/oblique
      - run: npm publish ./dist/oblique --provenance --access public
